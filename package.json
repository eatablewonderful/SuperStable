{
  "name": "superstable",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "@openzeppelin/contracts": "^4.4.2"
  },
  "devDependencies": {},
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate --reset"
  }
}
